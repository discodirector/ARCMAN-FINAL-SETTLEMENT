// Community Levels - User-submitted levels approved by developer
// Add approved community levels here

// Level format structure (same as DEFAULT_LEVELS):
// {
//   id: number,
//   name: string,
//   player: { x: number (0-1 relative), y: number (0-1 relative) },
//   arcGates: [{ x, y, width, height (all 0-1 relative), active: true, rotation: 0 }],
//   slippageClouds: [{ x, y, radius (all 0-1 relative) }],
//   lifeRestores: [{ x, y, radius (all 0-1 relative) }],
//   barriers: [{ x, y, width, height (all 0-1 relative), size: "large"|"medium"|"small", rotation: 0 }],
//   settlementZone: { x, y, width, height (all 0-1 relative), rotation: 0 }
// }

const COMMUNITY_LEVELS = [
    // Add approved community levels here
];

