{
  "name": "arcman-chain",
  "version": "1.0.0",
  "private": true,
  "description": "ARCMAN contracts: reward pool and its tests",
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  }
}
